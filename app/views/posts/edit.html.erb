<%= form_with model: @post, url: beer_post_path(@beer, @post), method: :patch do |f| %>
    <%= render "partials/errors", errors: @post&.errors&.full_messages %>
    
    <%= f.label :text_area %>
    <%= f.text_field :text_area %>
  
    <%= f.submit %>
  
<% end %>